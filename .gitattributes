# Keep shell scripts LF so bash/shellcheck work on every platform.
*.sh        text eol=lf
install.sh  text eol=lf

# PowerShell is happiest with CRLF on Windows.
*.ps1       text eol=crlf
*.psd1      text eol=crlf

# Everything else: normalize to LF in the repo.
*           text=auto eol=lf
